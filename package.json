{
  "name": "build-stack-journal",
  "version": "1.0.0",
  "description": "Build Stack Journal blog with a dependency-free Node backend.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "deploy": "wrangler deploy",
    "db:seed": "wrangler d1 execute blog-db --remote --file=seed.sql -y"
  },
  "keywords": [
    "blog",
    "engineering",
    "node"
  ],
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
